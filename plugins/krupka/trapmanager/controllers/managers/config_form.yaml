# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Manager

# Model Form Field configuration
form: $/krupka/trapmanager/models/manager/fields.yaml

# Model Class name
modelClass: Krupka\TrapManager\Models\Manager

# Default redirect location
defaultRedirect: krupka/trapmanager/managers

# Create page
create:
    title: backend::lang.form.create_title
    redirect: krupka/trapmanager/managers/update/:id
    redirectClose: krupka/trapmanager/managers

# Update page
update:
    title: backend::lang.form.update_title
    redirect: krupka/trapmanager/managers
    redirectClose: krupka/trapmanager/managers

# Preview page
preview:
    title: backend::lang.form.preview_title
